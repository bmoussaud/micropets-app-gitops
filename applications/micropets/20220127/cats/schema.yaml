#@data/values-schema
---
application:
  name: name
service:
  name: name
  port: 0
  image: image
  replicas: 1
  config_path: config_path
  config_filename: config_filename   
  backend:
    frequencyError: 0
    observability: true
    mode: RANDOM_NUMBER                            
environment:
  name: name
  ingress: ingress
  domain:
    internal: internal
    exposed: exposed

